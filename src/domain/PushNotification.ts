import { NotificationRecord } from '../../../specs/NativeNotificationHistory';

// Несмотря на то, что типы одинаковые,
// лучше сохранить принцип разделения ответсвенности

export type PushNotification = NotificationRecord;
